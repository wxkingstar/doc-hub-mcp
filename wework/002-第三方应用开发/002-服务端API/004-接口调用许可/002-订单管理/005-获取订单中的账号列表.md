---
title: "获取订单中的账号列表"
doc_id: 38201
category_id: 95649
source_url: https://developer.work.weixin.qq.com/document/path/38201
---
查询指定订单下的平台能力服务账号列表。若为购买账号的订单或者存量企业的版本付费迁移订单，则返回账号激活码列表；若为续期账号的订单，则返回续期账号的成员列表。注意，若是购买账号的订单，则仅订单支付完成时，系统才会生成账号，故支付完成之前，该接口不会返回账号激活码。
>如果是多企业订单，请先调用[获取多企业订单详情](#47530)获取到每个企业的子订单id (sub_order_id)，然后使用 sub_order_id 来调用此接口。

**请求方式：** POST（**HTTPS**）
**请求地址：** https://qyapi.weixin.qq.com/cgi-bin/license/list_order_account?provider_access_token=ACCESS_TOKEN

**请求包体：**

```
{
  "order_id" : "XXXXXXXX",
  "limit":1000,
  "cursor":"xxxx"
}
```

**参数说明：** 

| 参数 | 是否必须 | 说明 |
| ------- | ---- | ---- |
| provider_access_token | 是 | 应用服务商的接口调用凭证，获取方法参见[服务商的凭证](#15143 ) |
| order_id | 是 | 订单号 |
|limit|否|返回的最大记录数，整型，最大值1000，默认值500|
|cursor|否|用于分页查询的游标，字符串类型，由上一次调用返回，首次调用可不填|

**返回结果：** 
```
{
  "errcode": 0,
  "errmsg": "ok",
  "next_cursor": "xxxx",
  "has_more":1,
  "account_list":[
    {
      "active_code": "code1",
      "userid":"XXX",
      "type": 1
    },
    {
      "active_code": "code2",
      "userid":"XXX",
      "type": 2
    }
  ]
}
```

**参数说明：** 

| 参数 | 说明 |
| ------- | ---- |
| errcode |错误码 |
|errmsg|错误码说明|
|next_cursor|分页游标，再下次请求时填写以获取之后分页的记录|
|has_more|是否有更多。 0: 没有， 1: 有|
|account_list|账号列表|
|account_list.active_code|账号码，订单类型为购买账号时，返回该字段|
|account_list.userid|企业续期成员userid，订单类型为续期账号时，返回该字段。返回加密的userid|
|account_list.type|账号类型：1:基础账号，2:互通账号|
