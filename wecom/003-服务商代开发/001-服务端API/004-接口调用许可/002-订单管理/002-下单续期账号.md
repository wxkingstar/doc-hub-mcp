---
title: "下单续期账号"
doc_id: 38197
category_id: 97183
source_url: https://developer.work.weixin.qq.com/document/path/97183
---
最后更新：2024/01/31

[TOC]

可以下单为一批已激活账号的成员续期，续期下单分为两个步骤：
1. 传入userid列表创建一个任务，创建之后，可以往同一个任务继续追加待续期的userid列表；
2. 根据步骤1得到的jobid提交订单。

## 创建续期任务
在同一个订单里，首次创建任务无须指定jobid，后续指定同一个jobid，表示往同一个订单任务追加续期的成员。

**请求方式：** POST（**HTTPS**）
**请求地址：** https://qyapi.weixin.qq.com/cgi-bin/license/create_renew_order_job?provider_access_token=ACCESS_TOKEN

**请求包体：**

```
{
  "corpid" : "wwxxx",
  "account_list":[
    {
      "userid":"userid1",
      "type":1
    }
  ],
  "jobid":"JOBID"
}
```

**参数说明：** 

| 参数 | 是否必须 | 说明 |
| ------- | ---- | ---- |
| provider_access_token | 是 |应用服务商的接口调用凭证，获取方法参见[服务商的凭证](#15143 ) |
|corpid|是|企业id|
|account_list|是|续期的账号列表，每次最多1000个。同一个jobid最多关联1000000个基础账号跟1000000个互通账号|
|account_list.userid|是|续期企业的成员userid。|
|account_list.type|是|续期账号类型。1:基础账号，2:互通账号|
|jobid|否|任务id，若不传则默认创建一个新任务。若指定第一次调用后拿到jobid，可以通过该接口将jobid关联多个userid|

**返回结果：**
```
{
  "errcode": 0,
  "errmsg": "ok",
  "jobid": "xxxx",
  "invalid_account_list":[
    {
      "errcode": 1,
      "errmsg": "xxx",
      "userid":"userid1",
      "type":1
    }
  ]
}
```

**参数说明：** 

| 参数 | 说明 |
| ------- | ---- |
| errcode |错误码 |
|errmsg|错误码说明|
|jobid|任务id，请求包中未指定jobid时，会生成一个新的jobid返回|
|invalid_account_list|不合法的续期账号列表|
|invalid_account_list.errcode|账号不合法相关错误码|
|invalid_account_list.errmsg|账号不合法相关错误描述|
|invalid_account_list.userid|用户userid|
|invalid_account_list.type|续期账号类型。1:基础账号，2:互通账号|

## 提交续期订单
创建续期任务之后，需要调用该接口，以提交订单任务。注意，提交之后，需要到服务商管理端发起支付，支付完成之后，订单才能生效。也可以通过接口[使用余额支付订单](#50103)。

**请求方式：** POST（**HTTPS**）
**请求地址：** https://qyapi.weixin.qq.com/cgi-bin/license/submit_order_job?provider_access_token=ACCESS_TOKEN

**请求包体：**

```
{
  "jobid" : "wwxxx",
  "buyer_userid":"xxxx",
  "account_duration":
  {
    "months":2
    //"new_expire_time":1700000000
  }
}
```

**参数说明：** 

| 参数 | 是否必须 | 说明 |
| ------- | ---- | ---- |
| provider_access_token | 是 |应用服务商的接口调用凭证，获取方法参见[服务商的凭证](#15143 ) |
|jobid|是|任务id|
|buyer_userid|是|下单人，服务商企业内成员的明文userid。该userid必须登录过企业微信，并且企业微信已绑定微信，且必须为服务商企业内具有“购买接口许可”权限的管理员。|
|account_duration|是|账号购买时长|
|account_duration.months|否|与new_expire_time二者填其一。购买的月数，每个月按照31天计算。最多购买60个月。(若企业为服务商测试企业，每次续期只能续期1个月)|
|account_duration.new_expire_time|否|与months二者填其一。指定的新到期时间戳，不可为今天和过去的时间，不可为1860天后的时间。须填当天的24时0分0秒，否则系统自动处理为当天的24时0分0秒。(若企业为服务商测试企业，不支持指定新的到期时间来续期)|

**返回结果：**
```
{
  "errcode": 0,
  "errmsg": "ok",
  "order_id": "xxxx"
}
```

**参数说明：** 

| 参数 | 说明 |
| ------- | ---- |
| errcode |错误码 |
|errmsg|错误码说明|
|order_id|订单号|
